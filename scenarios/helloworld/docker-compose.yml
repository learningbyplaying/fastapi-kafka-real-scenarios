version: '3'

services:

 producer:

   build: ./producer
   volumes:
     - ./producer:/app
   ports:
     - "80:80"
   environment:
     - PORT=80
   command: uvicorn main:app --host 0.0.0.0 --port 80 --reload
