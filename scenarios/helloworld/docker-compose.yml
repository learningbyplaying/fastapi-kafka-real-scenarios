version: '3'

services:

 producer:

   build: ./producer
   ports:
     - "80:80"
   volumes:
     - ./producer:/app
   environment:
     - PORT=80

   command: uvicorn main:app --host 0.0.0.0 --port 80 --reload
